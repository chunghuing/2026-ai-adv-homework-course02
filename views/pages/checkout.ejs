<div id="app">
  <h1 class="text-2xl font-bold text-ink mb-6" style="font-family: 'Noto Serif TC', serif;">結帳</h1>

  <div v-if="loading" class="flex justify-center py-12">
    <div class="w-8 h-8 border-4 border-ink border-t-transparent rounded-full animate-spin"></div>
  </div>

  <div v-else class="flex flex-col lg:flex-row gap-8">
    <!-- Left: Form -->
    <div class="lg:w-3/5">
      <div class="bg-paper-light border border-line p-6">
        <h2 class="font-semibold text-fg-primary mb-4" style="font-family: 'Noto Serif TC', serif;">收件資訊</h2>
        <div class="space-y-4">
          <div>
            <label class="block text-sm text-fg-secondary mb-1">收件人姓名</label>
            <input
              v-model="form.recipientName"
              type="text"
              placeholder="請輸入收件人姓名"
              class="w-full border border-line px-3 py-2.5 text-sm focus:outline-none focus:border-ink transition-colors bg-paper-light"
              :class="{ 'border-mulberry': errors.recipientName }"
            />
            <p v-if="errors.recipientName" class="text-mulberry text-xs mt-1">{{ errors.recipientName }}</p>
          </div>
          <div>
            <label class="block text-sm text-fg-secondary mb-1">Email</label>
            <input
              v-model="form.recipientEmail"
              type="email"
              placeholder="請輸入 Email"
              class="w-full border border-line px-3 py-2.5 text-sm focus:outline-none focus:border-ink transition-colors bg-paper-light"
              :class="{ 'border-mulberry': errors.recipientEmail }"
            />
            <p v-if="errors.recipientEmail" class="text-mulberry text-xs mt-1">{{ errors.recipientEmail }}</p>
          </div>
          <div>
            <label class="block text-sm text-fg-secondary mb-1">收件地址</label>
            <input
              v-model="form.recipientAddress"
              type="text"
              placeholder="請輸入收件地址"
              class="w-full border border-line px-3 py-2.5 text-sm focus:outline-none focus:border-ink transition-colors bg-paper-light"
              :class="{ 'border-mulberry': errors.recipientAddress }"
            />
            <p v-if="errors.recipientAddress" class="text-mulberry text-xs mt-1">{{ errors.recipientAddress }}</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Right: Summary -->
    <div class="lg:w-2/5">
      <div class="bg-paper-light border border-line p-6 sticky top-24">
        <h2 class="font-semibold text-fg-primary mb-4" style="font-family: 'Noto Serif TC', serif;">訂單摘要</h2>
        <div class="space-y-3 mb-4">
          <div v-for="item in cartItems" :key="item.id" class="flex justify-between text-sm">
            <span class="text-fg-secondary">{{ item.product.name }} x {{ item.quantity }}</span>
            <span class="text-fg-primary">NT$ {{ (item.product.price * item.quantity).toLocaleString() }}</span>
          </div>
        </div>
        <div class="border-t border-line pt-3 space-y-2 text-sm">
          <div class="flex justify-between">
            <span class="text-fg-secondary">商品小計</span>
            <span>NT$ {{ cartTotal.toLocaleString() }}</span>
          </div>
          <div class="flex justify-between">
            <span class="text-fg-secondary">運費</span>
            <span>{{ cartTotal >= 500 ? '免運' : 'NT$ 150' }}</span>
          </div>
        </div>
        <div class="border-t border-line pt-3 mt-3 flex justify-between items-center">
          <span class="font-bold text-fg-primary">總計</span>
          <span class="font-bold text-xl text-mulberry" style="font-family: 'Fraunces', serif;">
            NT$ {{ (cartTotal + (cartTotal >= 500 ? 0 : 150)).toLocaleString() }}
          </span>
        </div>
        <button
          @click="submitOrder"
          :disabled="submitting"
          class="w-full mt-4 bg-ink text-paper-light py-3.5 rounded text-sm font-medium hover:bg-ink-soft transition-colors disabled:opacity-50"
        >
          {{ submitting ? '處理中...' : '確認送出訂單' }}
        </button>
      </div>
    </div>
  </div>
</div>
